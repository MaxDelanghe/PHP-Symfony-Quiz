{% extends 'base.html.twig' %}

{% block title %}Mise a jour{% endblock %}

{% block body %}
    <h3>Mise a jour de votre email</h3>

    {{ form_start(form) }}
        {{ form_row(form.email, {'attr': {'placeholder': user.email}}) }}
        <button type="submit" class="btn btn-primary">Update</button>
    {{ form_end(form) }}
{% endblock %}